export const className = 'ppi-style-flat'
export const css = `
/* Solid filled container — background = currentColor, icon forced white */
.ppi-style-flat {
  border-radius: 14px;
  padding: 14px;
  background: currentColor;
}

.ppi-style-flat > svg {
  color: #fff;
  stroke: #fff;
}
`
